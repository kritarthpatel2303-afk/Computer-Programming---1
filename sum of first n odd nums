#include<stdio.h>
int main()
{
    int n;
    printf("Enter n: ");
    scanf("%d",&n);
    int sum = 0, odd =1;
    for (int i = 0; i< n; i ++){
        sum += odd;
        odd += 2;
    }
    printf("sum of first %d odd numbers = %d\n", n, sum);
    return 0;
          }
